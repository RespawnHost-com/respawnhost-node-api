# RespawnHost API Configuration
# Get your API key from https://respawnhost.com/api-keys
RESPAWNHOST_API_KEY=your_api_key_here

# Optional: Set custom base URL (defaults to https://respawnhost.com)
# RESPAWNHOST_BASE_URL=https://respawnhost.com

# Optional: Set a test server UUID for destructive tests
# Get UUID from https://respawnhost.com/servers
TEST_SERVER_UUID=your-server-uuid-here

# Optional: Enable destructive API calls (use with caution!)
# When set to 'false', only read operations will be executed
# When set to 'true', write/update/delete operations will also be executed
ENABLE_DESTRUCTIVE=false
